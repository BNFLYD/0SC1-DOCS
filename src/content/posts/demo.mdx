export const meta = {
  title: "Demo",
  subtitle: "Osea es una prueba",
  date: "2025-08-14",
  tags: ["prueba", "mdx", "react"],
  icon: "game-icons:steampunk-goggles"
  }

# Esto es un archivo mdx

## Esto es un subtitulo

### Esto es un subtitulo mas chico

#### Y hay como 2 mas despues de este pero la verdad que no tiene sentido usar mas alla de este porque son imperseptibles con los parrafos

Esto es un parrafo: Waos, impresionante! Casi que parece que es un formato de texto plano que podemos usar para hacer notas o cosas asi (pd: aguante Logseq).

* Esto es una lista

- **Esto es una lista con negrita** (si tocas el triangulito explota)
  - Bueno te menti, me dio paja animar la explocion. Esto es una lista anidada dentro de la lista con negrita
    - Y esto es otra lista anidada, dentro de la lista anidada, dentro de la lista con negrita
      - creo que se entiende la idea no? ya no quiero anidar mas listas, porfavor.
      - Osea podes hacer listas anidadas y poner lo que queras dentro de ellas literalmente, mas listas, imagenes, codigo, memes, una tesis completa sobre como centrar un div y luego esconderlo todo bajo la alfombra para que nadie sospeche del semejante acto de desprecio por la humanidad que acabas de escribir. O lo que sea (siempre y cuando sea texto plano, osea es un formato de texto plano no una pantalla magica, no soy Willy Wonka, la concha de tu madre).

Tambien podes hacer cuadros de codigo identado simplemente usando ```  (se llaman backticks) poniendo tu codigo y cerrando con mas backticks, osea esa es la sintaxis, en realidad se ve asi:

```js title="ejemplo.js"
// Este codigo se identa segun el lenguaje en este caso JS

export const PI = 3.14159
const enabled = true
let count = 0
var legacy = null
const notANumber = NaN

const str = "Texto de ejemplo"
const regex = /foo(bar)?/gi

function add(a, b) {
  const obj = { key: "value", nested: { inner: 123 } }
  return a + b
}

const result = add(1, 2)
class Person {
  constructor(name) { this.name = name }
  greet() { return `Hi, ${this.name}` }
}
const user = new Person("Ado")

function Card({ title, children }) {
  return (
    <section className="card">
      <h2>{title}</h2>
      <div>{children}</div>
    </section>
  )
}

const App = () => (
  <Card title="Demo">
    <button onClick={() => console.log(user.greet())}>Click</button>
  </Card>
)

export default App
```
Te gusta el tema del identador? lo hice basado en la paleta de colores de la Sekaide, bastante masculino a mi parecer.

Tambien podes hacer lineas de comandos
```bash title=""
npm run dev #comando random
```
Super util para documentacion y tutoriales.

Ademas podes poner imagenes: ![img](../src/assets/hornero.svg)
Gif: ![gif](../src/assets/vaporwave.gif)
